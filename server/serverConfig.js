module.exports = require("../serverConfig.js")

// It requires a serverConfig.js file stored outside the "server" folder,
// so that when deploying a new server version, the current config file
// from the server is not overwritten.
